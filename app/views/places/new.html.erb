<h1>New Place</h1>

<%= form_with(model: @place, local: true) do |form| %>
  <div class="form-group">
    <%= form.label :name %>
    <%= form.text_field :name, class: 'form-control', id: 'name_input' %>
  </div>
  <button type="submit" class="btn btn-primary">Save</button>
<% end %>
